# wiki/ — 一层知识库

- **正文与 YAML 你做主**（审 LLM diff 时合并并改写）  
- **掌握度**：YAML，Dashboard 查询  
- **无 inbox**：临时速记若需要，放库根 `inbox/`（与 wiki、LLM 无关）  

## YAML

`kind` · `status` · `understanding_level`(0–5) · `need_practice` · `last_review` · `source` · `source_type` · `tags`

## 目录

`concepts/` `entities/` `comparisons/` `decisions/` `practice/` + 根下 note/essay + 索引三页

## LLM

只出 `diff/`；人审后写入 wiki。见 `AGENTS.md`
