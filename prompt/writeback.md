# 回写提示词（Write-back Prompt）

优质问答结论沉淀进 wiki 时使用。

---

用户认可了下列结论。请：

1. 指出应更新的既有 `wiki/` 页；确无合适页才新建（遵守 AGENTS.md）  
2. 改写成可复用知识，去掉闲聊口吻  
3. 标注来源：对话沉淀+日期，或 `[[raw/...]]`  
4. YAML 建议中 `last_review` 用今天日期；**不要改**用户已填的 `understanding_level` / `need_practice`（除非用户明确要求）  
5. 只写 `diff/`，等待人审后再合入 wiki  

用户认可的内容：

```
（粘贴）
```
