# 回写提示词（Write-back Prompt）

优质问答/对比结论沉淀进 wiki 时使用。

---

用户认可了下面这条结论/对比/清单。请：

1. 判断应更新哪个既有 `wiki/` 页；没有合适页才新建（遵守 AGENTS.md）。
2. 以「可复用知识」改写，去掉闲聊口吻与一次性上下文。
3. 标注来源：若来自某次对话，写明「对话沉淀 + 日期」；若能对应 raw，写 raw 路径。
4. 更新 `last_reviewed`。
5. 输出 `diff/`，等待人审。

用户认可的内容：

```
（粘贴）
```
